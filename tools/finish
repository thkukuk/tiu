#!/bin/bash

set -e

ROOTDIR=/mnt

# Make sure /usr is newer than /etc
touch ${ROOTDIR}/usr

btrfs property set ${ROOTDIR}/usr/ ro true

umount -R ${ROOTDIR}/usr

touch ${ROOTDIR}/usr

# Umount everything
umount -R ${ROOTDIR}
